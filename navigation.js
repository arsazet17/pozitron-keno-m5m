(function(){const order=['home','history','algorithm','archive','settings'],pages=[...document.querySelectorAll('.page')],buttons=[...document.querySelectorAll('.nav-btn')];function go(name){if(!order.includes(name))return;pages.forEach(p=>p.classList.toggle('active',p.dataset.page===name));buttons.forEach(b=>b.classList.toggle('active',b.dataset.target===name));history.replaceState(null,'','#'+name);scrollTo({top:0,behavior:'smooth'});}buttons.forEach(b=>b.addEventListener('click',()=>go(b.dataset.target)));const initial=location.hash.slice(1);if(order.includes(initial))go(initial);window.M5Nav={go};})();
